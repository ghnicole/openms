# Offline field combat and progression

Current damage calculations follow the [modern combat rules](combat-formulas.md) selected by the user. Historical v83 formula descriptions below are superseded by that contract.

> [!NOTE]
> The offline client has been removed. This page records historical local-client behavior. Current combat authority belongs to the server and is summarized in [online feature coverage](server/offline-parity.md).

## Authority and evidence boundary

`OfflineField` is an explicitly **offline-local-policy** authority, not a recovered server implementation. Original placements, templates, statistics, sword rectangles, mob bodies, attack areas and frame delays supply inputs. Ordinary one-handed-sword output and physical/magical incoming calculations use recovered client consumers; local rules still own autonomous AI, accepted HP debits, respawn and progression. Learned records and supported consumers are documented in [skills](skills.md). Per-mob drops use the authorized Cosmic **server reference** with original WZ metadata/artwork and local durable pickup authority, not Nexon drop data or server parity. Missing controllers and scripts are not replaced with fake content.

Recovered consumers are retained in `ghidra-ingame-life/`, `ghidra-physics-hitboxes/`, and [the client-correction export provenance](ghidra-client-corrections/provenance.json):

| Evidence                                       | Used behavior                                                                                                                                     | Not inferred                                                                                                                |
| ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| `0067cf06`; `0067da22..0067e078`               | Mob template/link loader; original `maxHP/maxMP`, damage stats, EXP and recovery properties. Speed inputs have 100 added at `0067db72..0067dcbe`. | Original physical speed formula, AI, damage equation, recovery cadence.                                                     |
| `00664559`, `00663fe2`                         | Current frame's explicit `lt/rb`, mirrored/translated; swept rectangle uses the same current frame at previous position.                          | Visual canvas bounds are never combat hitboxes.                                                                             |
| `0045183b`, `00af14b8`, `00af14c8`             | Existing standing/prone ordinary player receiver implementation.                                                                                  | No morph, mount or learned skill is automatically equipped.                                                                 |
| `0040ac78`, `00414440`, `00950921`, `0078df87` | Equipped weapon afterimage `lt/rb`, facing/outgoing area and the supported one-handed-sword per-target generator.                                 | Original hit scheduling, PRNG seed and unsupported weapon/status branches are not claimed.                                  |
| `0067fd81`, `0066a517`, `0066d9c0`, `0066de86` | Original type-0 rectangle, `attackAfter`, `conMP`, `magic` properties; attack delay is honored at the first fixed tick reaching it.               | The original numerous eligibility gates are not reconstructed; selection/eligibility are labeled local.                     |
| `006d9993`, `006d9549`, `006d041e`, `006d089a` | Original network/pool actor construction exists.                                                                                                  | Authored map records are not claimed to reproduce original live spawn/controller packets.                                   |
| `009581a9`, `00930b27`, `007a6353`             | Ordinary incoming-hit admission, signed 1500-ms timer, RGB blink and contact-detaching impulse.                                                   | Untyped special status/deadline gates and linked attack-specific 900/1900 impulses are not generalized to ordinary attacks. |

A direct original WZ inspection read `Character.wz:Weapon/01302000.img/info`: `afterImage=swordOL`, `incPAD=17`, `attackSpeed=4`, `sfx=swordL`. `Character.wz:Afterimage/swordOL.img/0` contains `swingO1`, `swingO2`, `swingO3`, `swingOF`, `stabO1`, `stabO2`, `stabOF`, `proneStab`. For example, `swingO1` is exactly `[-88,-62]..[-18,-6]`; `proneStab` is exactly `[-74,-11]..[-15,1]`. The extractor retains every original action rectangle in this equipped bank. These are **not** the old swordOS inspector selection.

No EXP threshold table was recovered from the retained original consumers or the complete Etc IMG inventory. A search of the unpacked executable for the candidate initial integer sequence `15,34,57,92` found no contiguous match; this is not proof that no original table exists. Consequently progression is explicitly local, not a remembered MapleStory table presented as recovered evidence.

## Fixed-tick runtime

`new OfflineField(scene,store,hooks)` requires a prepared scene with `scene.simulation`, `scene.actor`, `manifest.life`, and `manifest.combat`. It attaches itself to `scene.offlineField` and exposes:

- `await prepare(signal)`: acquire original mob presentation only for the initial viewport plus half-viewport margin; failed/cancelled candidates release owned resources.
- `step(30,input)`: exactly one gameplay update per executed original 30-ms physics quantum. A non-30-ms step is an error. There is no second accumulator and no RAF gameplay timer.
- `action`, `playback`, `blocksMovement`, `dead`: avatar presentation and next-tick input-lock projection. Locks retain physics gravity/inertia. Attacks must pass through one idle/base-action tick before a new attack can start, so the idempotent animation API cannot hide a repeated same-action restart.
- `updateDemand()`: scene demand scheduler calls this outside simulation; at most two template loads may be pending.
- `snapshot()`: policy, current player/mob statistics, exact rectangle state, action/time, special restrictions, residency, and explicit unsupported capabilities.
- `destroy()`: idempotent. Removes display membership before destroying entities/subtextures, aborts pending work, and releases leases. Async completion cannot resurrect a destroyed owner.

Hooks observe accepted events, not predictions: kills, strikes, effects, profile changes, ordinary attacks, mob/player hits, HP recovery and player death. Kill/death notifications occur only at the lethal transition; reactor strikes occur once per committed impact. Field-owned `CombatPresentation` consumes hit/recovery outcomes; the persistent audio owner plays authored available cues. `onPlayerDeath()` clears active skill buffs and opens the ordinary revival confirmation through the UI/Main map-transition owner.

Tick order is original player physics (Main), ordinary receiver projection, mob timer/motion/frame/body updates, player input/action/impact, type-0 mob decisions/impacts and body contact, then alert resolution, signed hit-timer/tint update, passive recovery and movement-lock projection. All current gameplay state is independent of Pixi residency. `EntityAnimation.seek(mob.actionMs)` projects already-decided original artwork timing; RAF must exclude `gameplayOwned` entities.

## Recovered ordinary player hit response

- `tryReceiveHit({amount,direction,locallyInitiated,source?,attackAction?})` is the shared outcome boundary for contact and authored attacks. Locally initiated hits reject a nonzero signed timer or dead player; already-authorized outcomes bypass those two guards. An ordinary attack pose does **not** confer incoming-hit immunity. Optional `attackAction` is accepted-impact provenance for audio, not damage authorization; contact clears it rather than borrowing a pending attack pose. Unsupported original status/deadline fields are not mapped to arbitrary local attack state.
- `009591fb..00959227` installs **+1500 ms for positive damage**, **−1500 ms otherwise**. Both signs block local admission. Each original 30-ms avatar update approaches zero by 30 before choosing tint; a newly admitted hit is therefore observed as 1470 ms at that quantum's draw.
- `00930b27` increments a persistent counter while the positive timer is active and the player is alive. Two of each four updates use RGB **`0x808080`**, the other two **`0xffffff`**. This is color modulation, not disappearance or half alpha. Nonpositive outcomes protect without blinking; expiry, death and recovery restore normal tint.
- Impulse eligibility is independent of HP loss and signed protection. Fresh [conditional-knockback instructions](ghidra-client-corrections/knockback-instructions.txt) correct the earlier unconditional200 interpretation: `00958670..79` zeros both impulse components and skips their producer for nonpositive damage; `00958686` initializes ordinary magnitude **270**, while `009589a7` replaces it with200 only in the positive Power Guard argument branch. Ordinary admitted direction therefore selects **(−270,−270)** or **(+270,−270)** pixels/s. The `0x7fffffff` sentinel yields zero components, and `009591c0..f6` calls the external impulse primitive only if either component is nonzero. Misses and the sentinel must not detach contact.
- The old 300-ms movement-locking hit phase is removed. An admitted positive hit selects the actor's original `hit` facial expression for1500ms; zero/negative outcomes do not start that expression. This distinct emotion deadline is not locomotion stun. Existing frame alpha remains intact; tint is applied on the actor parent and covers resident and subsequently selected parts. [Expression/carry instructions](ghidra-client-corrections/fidelity-r2-emotion-carry.txt) retain the original emotion branch.
- Autonomous mob decisions and progression remain explicitly local. Supported outgoing sword and ordinary incoming calculations are recovered client behavior, not server authorization. Ordinary passive recovery follows the recovered producer below; unmapped special states remain explicit.
- `00958ae2..c46` draws a native unsigned random value modulo100 and compares it with the clamped0..100 secondary-stat Stance probability. An active learned Stance buff supplies `hooks.derivedStats().stance`; resistance preserves position, velocity, foothold/ladder contact and the current action, but **not HP**. Expired/unlearned Stance is zero, not a job-based grant. The browser draws from the injected offline `[0,1)` gameplay stream as `floor(sample*2^32)%100`; it does not claim the original PRNG seed. `lastKnockback` and `lastKnockbackRoll` expose the admitted branch without inferring it from HP.
- Once admitted, `007a6353` detaches foothold/ladder contact, clears grounded velocity first, preserves stronger aligned airborne components and combines opposing components. Existing swept feet/foothold collision owns subsequent ground/air/wall motion; hitboxes and sprite bounds do not become terrain colliders. A refused impulse never enters that primitive, including while crouching or climbing.
- `hooks.absorbDamage(amount)` supplies Magic Guard's HP remainder after its owned MP debit; `hit.hpDamage` reports that remainder to presentation. Eligibility, Stance, expression and the signed hit timer use the original hit amount, so absorbing HP loss cannot manufacture a miss or suppress a separately admitted impulse.
- The native `+2dd8` state-gated alternate probability consumer (skills5110001/15100004), positive Power Guard reflection branch, special linked-attack900/1900 impulses and native field-mode bypass remain unmapped. They are not silently activated by ordinary attack, ladder, death or job labels.

The outer producer is now traced through `009f5c50 → 009f84d0 → 00531b8d → 00a03350 → 009717b5 → 0094a144 → 00930b27`: one counter increment per30ms update, not per render frame. Thus each two-tick gray/normal phase is **60ms**, independent of the signed1500ms protection interval. The separate5000ms alert deadline is not invulnerability. [Motion recovery](ghidra-client-corrections/motion-recovery.json) retains the exact exports. The reported excessively fast appearance remains a discrepancy to compare with an original recording, not grounds for an invented slower timer.

### Requested offline damage-only outcomes

The ordinary native selector is not a universal recoil lottery. `ghidra-client-features/status/actor-alert-wide-insns.txt` at `00958670..679` rejects nonpositive damage before constructing the270 impulse; `00958ae2..00958c49` tests actual Stance/charged Energy state; `00958c94..cc7` supplies the ordinary signed impulse unless the direction is `INT_MAX`. The5000-ms brace deadline is written later at `0095923f`, not read as recoil resistance. No baseline chance to resist ordinary direction-bearing damage was recovered from that complete selector.

To implement the requested “most ordinary mob hits recoil, occasional hits only damage” behavior, the browser now has an explicitly **offline-local-policy90% ordinary recoil rule**, not a recovered original formula. For a positive mob-sourced hit, let `r = nextUint32()%100`. Native active Stance/charged Energy resistance retains first priority (`r < clampedSkillChance`), followed by the native no-direction sentinel. Otherwise `r >= 90` keeps position, velocity, foothold/ladder contact and action and reports `lastKnockback="offline-recoil-resistance"`; `r < 90` admits the original±270/−270 impulse. Both branches still debit the same HP, install positive1500-ms protection, and start the hit expression/brace. Unbuffed ordinary mob hits therefore recoil90% and retain contact10%; with skill chance `s`, admitted recoil occupies `s <= r < 90`. No extra random word is consumed, no HP is turned into MISS, and the new policy is not applied to no-mob fall damage.

The recovered physical evasion calculation itself is unchanged: `E=max(0,clamp(playerEVA,0,999)-trunc(max(0,mobLevel-playerLevel)/2))`, `A=clamp(mobACC,0,999)`, `q=100*E/(4.5*A)`. Clamp `q` to2..80% for ordinary jobs or5..95% for thief families; native comparison order retains the lower bound for0/0. Evade iff `(word0%10000000)*0.00001 < q`. `0079286e` and `physical-incoming-evasion-constants.txt` retain those exact consumers/constants. Physical damage otherwise uses `trunc((PAD²*(0.8+0.05*u)/100 - physicalReduction)*(1-Invincible/100))`, where `u=(magnitudeWord%10000000)*0.0000001`; defense uses the already documented original job/level StandardPDD consumer. A zero/negative physical proposal remains a legitimate non-damaging outcome for mobs far below the defender, independent of the new recoil rule.

**Local minimum-chip policy (explicitly requested, not recovered):** equipment defense zeroes the weakest same-level mobs — Snail `PAD12` computes ~1.2 before `physicalReduction`, so a beginner in the starter outfit (`PDD7`, StandardPDD `7`) recorded MISS on every contact. A non-evaded ordinary physical hit whose magnitude is `<= 0` now lands for **1** whenever `mobLevel + 10 >= playerLevel`; mobs more than ten levels below the defender keep the native non-damaging MISS. The evasion roll, its accuracy terms and the reduction formula are untouched, so stronger hits still compute their native magnitude.

A separate proven excessive-MISS defect affected learned Guardian: `00958763..771` compares **reserved incoming word3 modulo1000**, not a fresh word modulo100. Direct original `Skill.wz:112.img/skill/1120005/level` inspection gives rank1 `prop=5,time=2` and rank30 `prop=150,time=2`:0.5% and15%, not5% and100%. The consumer now uses the original denominator and reserved slot. Shadow Shifter likewise reuses reserved word1 (`00958592..59e`) instead of drawing another word. Guardian remains true evasion: `00958a9b→00958fe2` marks a negative missed outcome; it is not the requested HP-bearing recoil-resistance substitute. Four words are reserved at `009582d8..2f1`; an ordinary evaded outcome consumes only those four, magnitude consumes a fifth, and an admitted positive recoil decision consumes a sixth. These corrections preserve stream order for the following hit.

Deterministic regression recipe in `client/test/player-hit.test.js`: ordinary level1 profile STR12/DEX5/INT4/LUK4, zero defense baseline, mob level1/ACC20/PAD50, words `[9000000,0,0,0,9000000,89]` gives HP100→79 and(+270,−270); replacing89 with90 gives HP100→79 with original ground contact; replacing the first word with0 gives HP100 unchanged, negative1500-ms protection and no recoil draw. Guardian rank30 words3=149/150 exercise the strict per-thousand boundary. These are isolated fixture inputs, not native Windows recordings or original server authority. Integration validation is owned by Main; no browser pass is claimed by these recipes.

A scoped in-memory production smoke executed `PhysicalDamage.receive`, `SkillDefenses` and `OfflineField.tryReceiveHit` against original physics globals and a synthetic flat foothold, without running tests/build/lint during concurrent edits. It observed exactly HP79/air/(270,−270)/timer1500/six draws for89; HP79/ground/foothold1/(0,0)/timer1500/six draws for90; and HP100/ground/timer−1500/four draws for true EVA. A separate production Guardian smoke observed word149→damage0 and word150→damage21, both consuming only the reserved four words plus magnitude. Presentation callbacks were isolated fixtures; native browser visual confirmation and integration test execution remain Main-owned.

## Recovered hit presentation

- `0066b05e` timestamps a local attack; `006675a8` retains the ordinary mob name for5000ms. Nonempty names require no `hideName` and no `HPgaugeHide`; `hideHP` is separate and no blanket boss exception is invented. `damagedByMob` has a different original persistent-gauge path and receives no synthesized ordinary timed name. `OfflineMobRenderer` is the sole gameplay mob-name owner; LifeSystem labels only NPCs.
- `00437d0f`/`0043852c` numbers retain exact `Effect/BasicEff.img/No*` glyphs: suffix1 for the leading numeral, suffix0 thereafter, origin-aware overlap, 400ms opaque plus600ms fade, and30px upward travel over1000ms. Outgoing positive is Red; zero is Red/Miss; outgoing negative magnitude is Blue.
- **Received damage is not a positive vital-number argument.** [Fresh instructions](ghidra-client-corrections/fidelity-core-sign.txt) at `009593a1..b2` subtract damage, add absorbed amounts, then call `0092ec50`. The ordinary unabsorbed positive hit therefore emits negative magnitude through **NoViolet**, not the healing **NoBlue** family. Zero uses Violet/Miss. Native ordinary HP recovery calls that consumer with a positive amount and is Blue; MP recovery emits no number. This supersedes the incorrect first-pass description in older combat-summary artifacts.
- The producer's `00959389..8e` rejects negative hit outcomes before drawing; these do not become blue healing numbers. `onPlayerHit` skips them. This is distinct from a negative signed **vital-number** argument produced by ordinary positive damage.
- Emission snapshots a fixed world anchor; later movement never reattaches the number. Native `004519aa` queries two current avatar binding canvases and takes the maximum positive vertical accessor. [Accessor recovery](ghidra-client-corrections/fidelity-core-height.txt) resolves the calls to canvas vtable `+0x74` and layer `+0x100`; the browser still approximates this with current composed-frame top. Exact two-native-layer equivalence remains unproven. Pool capacity64, overlay-front ordering and browser rasterization are explicitly presentation policy.

Ordinary player-attacked mobs are a separate receiver. [Interaction evidence](ghidra-client-corrections/interaction-skill-combat-provenance.json) traces `pushed` (absent default1) through `0067e029..0067e078`, `0066fb4d` and `00671210`. Subthreshold damage reduces HP without forcing reaction. [State instructions](ghidra-client-corrections/knockback-item-loader.txt) separate **pose, motion and damage**: `00668d51` refuses another reaction during the full authored hit deadline; the former1000-ms cap was incorrect. `0066b7c6..822` preserves an existing attack/skill pose and pending impact. A nonlethal reaction needs original `hit1` artwork. `0066b6fc` supplies ordinary velocity130 and braking force40000; `009bbdfd` applies mass100,30-ms steps (or a final shorter step), clamps through zero and midpoint-integrates scalar speed along the current foothold. `009b1646` projects that distance and speed through the normalized foothold tangent; horizontal travel is shorter on a slope. The separate no-foothold mode-3 branch integrates world X/Y and must not be used for grounded recoil. Connected floors can cross patrol `rx0/rx1`, but not unsupported floor ends. Full airborne/flying/special mob controllers remain unavailable. The recovered instructions are retained in [knockback-trajectory.txt](ghidra-client/knockback-trajectory.txt).

The mob blink branch `006686d5` requires `damagedByMob` and its own hit deadline/counter. It is not ordinary player-hit invulnerability and is not applied to every damaged mob. Ordinary mobs play authored `hit1`; players retain the positive-only **60-ms gray /60-ms normal phases** above. [Fresh blink instructions](ghidra-client-corrections/interaction-blink-cadence.txt) found no slower rate gate. The reported excessively fast appearance remains an unresolved original-runtime comparison, not permission to invent a slower timer.

The random branch at `009523aa..00952419` is **strong recoil selection, not a universal chance to react**. The equipped weapon's original `info/knockback` (string2039/0x7f7, loader `005cc17c..1ce`, absent default0, template+134) reaches `008ed0ab`; a modulo100 roll below its clamped percentage selects reaction type2. `0066b6fc` type2 uses300px/s and force20000 (200px/s² at mass100), versus ordinary type1's130/400. Both still require the damage threshold for the currently supported sword consumers. The starter01302000 has no `knockback`, so it always uses ordinary recoil when otherwise admitted; no invented50% admission replaces it. Native skills3101003/3201003 can add probability and override a subthreshold nonboss outcome, and `00671210` scales a nonboss threshold with native field+B2C percent; neither consumer is active in the current fixed-sword scope. Bosses are not given blanket ordinary recoil immunity.

Do not transfer gates across controller authorities: the alternate `0066fb4d` reaction consumer refuses current native actions6..8,12..20 and21..37; its boss predicate `0066918f` adds a10000-ms timestamp gate. The ordinary local-player `00668b83` path instead has ownership/controller and native+334/+524 restrictions plus the hit-deadline gate mapped above. Those special state flags and airborne/flying trajectory controllers remain semantically unavailable, not arbitrarily equated to every browser mob's `hit`/`attack` state.

## Recovered natural recovery

`PassiveRecovery` is owned by `OfflineField` and executes after the current physics/contact outcome, once per30ms quantum. [Producer/eligibility](ghidra-client-corrections/regen-consumer.txt), [instructions](ghidra-client-corrections/regen-insns.txt), and [formulas](ghidra-client-corrections/regen-formulas.txt) establish independent ordinary timers: HP10 and MP3 every10000ms, multiplied by the map's original `info/recovery` (absent ordinary multiplier1) and truncated. There is no fabricated “three seconds since damage” delay or sitting +5/+3.

HP requires alive, below maximum, unchanged X/Y and an admitted standing/sitting action; movement or failed eligibility resets HP progress. MP requires alive and below its own maximum but continues through movement and actions; failed MP eligibility resets only its timer. Timers reset on firing, with no catch-up burst. HP presentation receives the producer amount, while durable HP/MP clamp to their maxima.

The **5000-ms alert/brace clock** is independent of hit protection, the1500-ms facial expression and both recovery accumulators. Positive admitted hits and admitted ordinary/skill attacks write it (`0095923f`, `0092edb2`). The [original transition](ghidra-client-features/status/alert-transition-core.txt) and [countdown/selector](ghidra-client-features/status/alert-countdown-selector.txt) show the30-ms decrement and negative-crossing clamp:5000 requires167 quanta to retire. Only grounded idle `stand1/stand2` becomes original `alert`; locomotion, airborne, crouch, ladder/rope, seat and existing attack artwork are not replaced. Brace never locks movement or extends invulnerability. `PassiveRecovery` receives the resolved action, so `alert` resets HP eligibility even at unchanged coordinates; MP continues independently.

The [native gameplay diagnostic](native-ui-validation/gameplay/report.json), `brace-natural-recovery-consumable`, used an explicit safe-ground profile fixture and a real attack. It observed MP10→13 by10050 simulated ms, ordinary standing HP eligibility beginning by5130ms, and HP20→30 at15120ms. Its contact scenario separately observed two positive hits1500ms apart, ordinary−270/−270 impulse and continued brace after hit protection expired. This is exact historical browser clock coverage, not a dedicated full post-hit regen isolation or Windows timing comparison.

Learned-only hooks `skillLevel(id)` and `skillInfo(id,rank)` supply actual durable ranks and WZ level fields; job never grants ranks. Warrior-family learned1000000 gives5000ms standing HP and its WZ`hp` bonus. Magician2000000 MP bonus is `trunc(characterLevel * learnedRank * 0.1)`. Mapped thief4100002/4200001 and knight1110000/11110000/1210000 bonuses use their selected WZ`hp`/`mp`. The recovered `007669dd` ancestry predicate is retained in [layer/predicate export](ghidra-client-corrections/fidelity-core-layer-init.txt). Stationary ladder/rope uses learned warrior1000002 or the mapped thief skill's WZ`time*1000`; no positive time means no climbing HP.

Native exclusion helpers `004f0d45/004f0d64`, temporary authored HP/MP secondary timers, action9/10 gated by native fields528/53c, multiplier1.5 gate2b74, and the separate character113 bonus state remain semantically unmapped. `recoveryExcluded()` may supply a proven exclusion; no unsupported state is guessed active. No chair bonus exists without an active owned chair metadata/controller. Cosmic's anti-cheat timing is not the client producer cadence.

`hooks.onRecovery(amount,simulation)` forwards HP only to `CombatPresentation.onRecovery`. Revival is a separate UI/map transition, not a natural-recovery timer.

The online authority uses the same independent MP timer while sitting on a chair or map seat, including at full HP. MP changes appear in the MP bar without floating recovery numbers. The focused `bun server/tools/check-chair.js --output /tmp/openms-chair` scenario uses an owned chair through native inventory input, checks MP recovery with full HP and preservation after reconnect, and checks the project-bar player count as another participant disconnects. It also checks the bar at 1280×800 and 800×600; existing extracted assets are reused.

## Player world name

`PlayerName(scene,store)` owns a persistent text/background attachment under the actor's native depth group, with position derived from the unflipped world vector. `step(resolution)` follows presentation projection and rebuilds geometry only when the current `store.profile.name` or backing density changes. Equipment replacement reparents field-owned attachments before destroying the old actor. The name reacquires the profile after atomic edits; `destroy()` releases its children. Unchanged RAF frames do not reconstruct text.

`00942dcc` invokes `005f0334` category1000 using user parent/layer offsets1150/11a4. The previous “unknown font provider” conclusion is narrowed by [compositor instructions](ghidra-client-corrections/fidelity-r2-name.txt): ordinary branch `005f10ce` pushes zero before `0098a707`. [Fresh slot0 instructions](ghidra-client-corrections/fidelity-core-font.txt) select string1597 (Arial), size12, ARGBffffffff. Native default background is a0000000.

The later [name layout instructions](ghidra-client-corrections/fidelity-name-layout.txt) resolve the earlier partial scan's vertical-placement uncertainty: `005f1691` pushes **−2** to the canvas-origin setter, and `005f17d1..1805` attaches the label to the supplied user vector. `00942df0` supplies user+11a4; `0092fdb0..fe11` queries that same vector for a world-coordinate foothold probe. The ordinary index0 label skips preceding-label stacking. Its world top-left is therefore `(userX−trunc(width/2), userY+2)`, not an offset from the current artwork's lower edge. Native measurement supplies width+5, font height+4, text at(2,0), and four transparent one-pixel corners. [Provenance and resolved geometry](ghidra-client-corrections/fidelity-name-provenance.json) retain the original input and export commands. Browser Arial measurement/rasterization and overlay-front depth remain implementation policies; no Windows pixel parity, optional nameTag/medalTag decoration or native hide/occlusion behavior is claimed.

## Explicit local rules

- One stable record is created for every authored mob placement. Authored hidden/limited/inactive entries are explicitly inactive. Missing positive maxHP or missing original action artwork is classified inactive rather than assigned synthetic values/artwork. Flying/special/missing-foothold movement is stationary, not fake ground AI. Selected-skill-only OctoPirates remain immune to the basic sword.
- Ordinary ground mobs initialize against their exact authored foothold, projecting authored x onto its finite original segment. They patrol only contiguous positive-x floor links within the authored `rx0..rx1`, turning at ranges, gaps, walls, unsupported links and plane/group changes. The original segment supplies y. A traversal has a 32-link bound; exhaustion records an explicit fault. No random AI, jump permission or flying trajectory is invented.
- Local horizontal speed is `60 * max(0,100 + original speed) / 100` pixels/s. Only the input normalization by 100 is original evidence; the conversion to 60 pixels/s and patrol rule are local. Ground admission requires original `move` artwork, an exact positive-x foothold and ordered finite range, with no positively identified flying controller. Nonzero `mobType` is not a stationary flag. `noFlip` locks artwork/body/attack mirroring, not movement direction.
- A new attack edge selects an available action from the original equipped weapon family, with crouched and close-target branches, only when equipment and prepared timing/geometry agree. Climbing rejects ordinary attacks; a held button does not repeat. Basic attacks cost no MP; ranged ammunition and presentation capacity are admitted before mutation. Original action timing drives the release, and ordinary ranged sound is dispatched there, not at admission.
- Learned claw projectile skills that use weapon ammunition, including Lucky Seven, retain the native close-attack branch. `SkillCombatController.prepareBasicFallback` checks the equipped claw, prepared actor/weapon, field/disease admission and selected melee rectangle before skill costs. A close eligible target, crouch or unavailable required rounds can select the ordinary claw melee attack; that branch spends no skill MP or stars and publishes no ranged skill projectile/cast effect. Learned-rank/job admission still applies; this is not a generic fallback for unsupported skills or weapons.
- `OfflineField.effectiveAttackSpeed()` applies actual temporary Booster/Speed Infusion values to the weapon speed, with Double Stab's own adjustment, clamped2..10. Magic instead uses6 plus Booster, not weapon speed or Speed Infusion. The admitted speed owns pose duration, animation projection and projectile release together; an active Claw Booster therefore accelerates the real ranged skill sequence rather than only its visible buff icon. Expiry/cancellation removes the temporary modifier without changing learned ranks.
- Basic attacks select the nearest eligible overlapping original current mob body, with field order breaking equal-distance ties; supported skill target counts use the same bounded nearest-first selection. [`PhysicalDamage`](../client/src/combat/physical-damage.js) now follows original `0078df87/00950921`: seven unsigned samples per target, the level-adjusted ACC/EVA check, learned mastery interval, `(4*rolledSTR+DEX)*PAD*0.01`, level-gap scaling, rolled50–60% target PDD subtraction and the supported skill percentage, then signed truncation. [Generator](ghidra-client-features/trading/damage-generator.txt) and [constants](ghidra-client-features/trading/damage-constants.txt) retain the binary evidence. The offline HP owner maps nonpositive proposed lines to zero debit/MISS and caps positive debit at remaining HP. The injected random stream is not native seed parity. The old deterministic `/10` formula and its STR125/DEX5→798 recipe are historical, not current damage guarantees.
- Nonlethal damage at or above original `pushed` admits available authored `hit1` reaction only after the separate hit deadline expires. It does not cancel an existing attack pose/pending impact. Ordinary grounded recoil starts at130px/s and brakes at400px/s²; the original weapon-probability type2 branch uses300/200. Motion needs at least90ms of authored hit duration and is independent of pose. Missing hit/die actions never fabricate frames. At zero HP the mob cannot contact, attack or be targeted; `die1` holds its final frame and disappears after its authored duration. Respawn waits at least10 seconds at the resolved original spawn (not a `mobTime=-1/0/1800` interpretation), resetting hit deadlines and previous/current coordinates together.
- Mob HP/MP initialize from original maxima. Every local five seconds, authored `hpRecovery/mpRecovery` values are applied up to maxima. Supported type-0 attack selection rotates through original attack records after a local 2400-ms cooldown, requires overlap and sufficient original `conMP`, consumes that cost, then applies the exact rectangle at the original `attackAfter`. Unknown types/rectangles/timing never generate fake attacks.
- Original `bodyAttack=1` enables local contact geometry. Incoming calculation reserves four random words and uses the recovered physical `0079286e` or magical EVA branch, with original mob level/accuracy. An evaded outcome gives zero damage and negative signed protection, not positive-hit impulse/blink.
- `PhysicalDamage.receive` replaces the former `PADamage/20` recipe. Original `0079309f/0079345e` use squared attack with an80–85% physical or75–80% magical roll, then matching defense reduction and signed truncation. Physical nonpositive results remain non-damaging; magic alone preserves the separate nonzero minimum1 (`00958422..34`). There is no universal positive1–199999 incoming clamp. `projectCharacterStats` supplies allocated, equipped and active temporary stats without double-counting defense. Physical reduction uses the six job-family/201-level tables extracted from **Base.wz:StandardPDD.img**, including original positive-value forward fill; magical reduction uses MDD and its primary-stat weights. Missing original damage/table inputs fail explicitly. The injected random stream and local HP authority are not native seed or server parity.
- At zero HP attack/locomotion are locked and the ordinary original revival confirmation opens. Jump does not revive and no guessed three-second local full-restore path remains. The confirmed transition follows the authored return map as described below.
- Killing awards original WZ `exp` once, then `awardExperience` applies the shared local threshold `15*level*level`, level cap200. Each level gives local base +5 maxHP and +3 maxMP plus supported learned vital-growth bonuses, full HP/MP, and [earned AP/current-job SP](character-gameplay-corrections.md#earned-points). Primary stats are allocated by the player. The helper also accepts a quest transaction draft and does not persist itself; the caller commits the profile and emits effects. Learned growth is not an original EXP/base-stat table, and job selection never grants a learned rank. The once-only lethal hook separately rolls the documented Cosmic-reference drop rows below.

## Ordinary revival confirmation

Original constructor `00897c94` uses `UIWindow.img/Notice/0`, a286×146 raster containing the original revival wording. `00898117..008981ba` places original `Basic.img/BtOK2` at `(124,115)`; the browser does not overlay duplicate rendered text. The modal cannot be dismissed to bypass death. `00898203` compares elapsed time with600000ms for the ordinary automatic confirmation; temporary input/transaction refusal leaves it eligible to try on a later update. [Constructor](ghidra-client-corrections/fidelity-revival-dialog.txt) and [events](ghidra-client-corrections/fidelity-revival-events.txt) retain the address-bearing evidence.

The authorized Cosmic **server reference**, not Nexon source, routes the ordinary branch in `ChangeMapHandler.java:105` through `map.getReturnMapId()`. `Character.java:7569–7576` changes map, cancels buffs and restores50 HP without changing MP in the no-charm branch. The browser uses the authored `returnMap` and portal0 arrival `(x,y-10)` through Main's ordinary atomic candidate load; the999999999 return sentinel denotes the same field. It never chooses a town by guessed geographic distance. HP clamps to the existing maximum. Failed loading retains the dead source field/prompt; HP is restored only with the successful commit.

EXP-loss, charm/wheel use and event-specific revival are not invented. The fixed-loadout ordinary local path keeps EXP unchanged; that is an explicit local boundary, not original death-penalty parity.

## Per-mob drops and durable pickup

[`drop-data.js`](../client/tools/drop-data.js) consumes every per-mob row in Cosmic's `src/main/resources/db/data/152-drop-data.sql` for authored placements in the selected original map closure, not a curated monster/item subset. The current **356-map release closure contains157 mob templates,1,643 drop item IDs and4,305 supported source rows; `unavailableRows=[]`**. Those are extraction-coverage counts, not kills or pickup acceptance. Source SQL SHA-256 is `f32135ffd9c4bfa4f57638f6499bdbc4c899076437cfd873def0901bb97534e6`; [drop provenance](ghidra-client-corrections/drop-provenance.json) records the full22,157-row input and original client/WZ presentation evidence.

Each row retains its source-row index, quantities, chance and quest ID. Item IDs join the original item-catalog dependency closure, including original metadata and immutable icon resources; missing templates/artwork, unrepresentable categories or invalid quantities remain explicitly unavailable rather than receiving substitute items. Zero unavailable source rows does **not** mean every item controller or scripted quest is supported. Equipment pickup creates a UID-bearing base-template inventory instance; original equipment selection is implemented separately, without Cosmic randomized instance statistics.

The roll follows Cosmic's `floor(random*999999) < effectiveChance`. Supported active monster-card modifiers and local family drop benefits multiply the packaged chance with Java-float rounding; they do **not** multiply meso/item quantities. Quantity is minimum-inclusive/maximum-exclusive; equal endpoints are a fixed local quantity. A nonzero quest ID requires a durable active quest at both roll and credit. Global/event drop controllers, network ownership/expiry and Nexon server authorization remain unavailable; supported card/family rates are not absent merely because they are locally owned.

`DropSystem` alone owns up to256 transient world records and one pending pickup. The once-only lethal hook supplies the mob identity/position; the renderer cannot roll, credit or delete loot. A batch is admitted only when capacity and authored supporting footholds suffice. Cosmic-reference25-pixel spread probes original floor segments below source y−85; a lateral point lacking ground uses the first supported drop point, while absence of source support rejects the batch explicitly.

Presentation now uses the [recovered drop-motion contract](drop-motion.md):30-ms original flight/fall, ordinary400px/s launch and100px apex,25px Cosmic-reference fanout,300-ms item rotation, original hover and currency-specific frame delays. `DropItem` plays on the first positive waiting-to-visible update with the strict `>300ms` cue throttle, **not at settlement**. The old local600-ms/48px launch and generic120-ms currency account are superseded. Only settled drops are pickable; original canvas origins/centering and foothold-derived depth remain presentation inputs, not credit authority.

The normal Pickup binding selects the nearest settled drop within the **local40-pixel horizontal/60-pixel vertical** reach, with stable slot order breaking ties. Dead, modal or transitioning input cannot start a pickup. Before publication, `creditDrop` checks current life/quest eligibility, original `only=1` uniqueness (including equipped ownership), original positive `slotMax`, inventory capacity and the2,147,483,647 meso cap. Missing `slotMax` uses explicit local100; the aggregate save accounts for96 slots per category, equipment one slot per item. These capacity/representation choices are local, not reconstructed native inventory layout.

Pickup preflights business rules, marks the world record pending, then rechecks against the atomic `commitProfile` draft. Durable credit must succeed before the original `PickUpItem` cue and recovered700-ms moving-player collection curve start. Refusal or save failure retains the unchanged durable inventory/meso and returns the entity to grounded availability; no success sound or double credit is emitted. Pending records are not expired while persistence is unresolved. Other records expire after180,000 simulated ms; this lifetime is local policy. Map replacement waits for pending transactions and discards uncollected field drops only after settlement, while accepted credits survive reload. Same-map relocation preserves the field/drop owner.

The [recovered drop-motion exports](drop-motion.md) now establish the supported original launch/fall/hover and local-player collection equations; richer packet modes still do not authorize missing server ownership. The [historical native pickup report](ingame-validation/interaction-corrections/report.json) proves its exercised browser transactions and older presentation, not current motion acceptance, Windows visuals, Nexon rates or server behavior.

### Tutorial drop admission and exact monster identity

Direct original `Map.wz`/`Mob.wz` inspection separates three cases; visual similarity and the word “tutorial” never choose another monster's table:

| Original authored placement                                                                | Authorized Cosmic per-mob rows                                                                                       | Offline admission                                                                                                                                             |
| ------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Map/Map0/000040000.img`, three `9300018` Tutorial Jr. Sentinels                           | SQL source row11179 (file line11180): item4031802, quantity1, quest1035, chance999999. No ordinary item or meso row. | Drops one Shellpiece only while Todd's Hunting Method is active; pickup rechecks that same durable quest.                                                     |
| `Map/Map0/000000002.img`, `9409000` Tutorial Leatty and `9409001` Tutorial Drumming Rabbit | No matching per-mob rows in the canonical SQL table.                                                                 | No invented loot and no borrowing ordinary Leatty/Rabbit tables. Absence of rows is server-reference coverage, not proof of an original client `noDrop` flag. |
| `Map/Map0/000050000.img`, ordinary `0100100` Snail                                         | 17 exact source rows, including Snail Shell4000019 at600000 and mesos at400000, minimum4/maximum-exclusive6.         | Normal independent probability rolls. A kill is not guaranteed to drop either reward; no tutorial bonus or blanket minimum is added.                          |

The missing intended Sentinel Shellpiece was an **upstream quest-admission defect**, not a missing drop table: `quest-data.js` classified every positive weighted completion reward as unsupported, making the entire1035 record unavailable even at Todd's start endpoint. Original `Quest.wz:Check.img/1035/0` requires beginner job0, level≤10, NPC2004; completion at NPC2002 requires one9300018 kill and one4031802. `Say.img/1035/0/yes/1` explicitly directs the player to pick up that Shellpiece. `Act.img/1035/1/item` authors five hats1002053/1002008/1002014/1002068/1002069, each `job=1,prop=1,count=1`, plus the Shellpiece debit−1. Those original properties must not block the otherwise supported quest or become five guaranteed hats.

Positive weights now admit the general weighted-reward consumer. The **authorized Cosmic server-reference** `server/quest/actions/ItemAction.java:96–125` supplies the selection policy: sum eligible positive weights, draw once in `[0,total)`, select the first cumulative weight exceeding the draw. Absent `prop` is unconditional; `prop=0` grants nothing; `prop=-1` remains an explicit player choice. One injected offline gameplay draw selects the reward inside the atomic profile transaction; dialogue/readiness inspection and quest start with no weighted rewards consume no draw. Source weights and job masks are retained; this is not a recovered Nexon server RNG or drop-rate claim. Unrepresentable weights, unsupported scripts and other original mandatory gates remain unavailable.

The retained client evidence remains the original template/link loader `0067cf06` and native drop presentation/pickup consumers described above; none supplies server loot tables. The existing drop-table projection and all capacity, quest-at-credit, item-instance, meso-cap and pickup-save contracts are intentionally unchanged. Packaged development-spawn mob templates already share the complete catalog table set; item artwork remains demand-loaded by `DropRenderer`, not replaced by a synthetic drop producer.

Focused regression recipe: `bun test client/test/quest-weighted-rewards.test.js client/test/drop-system.test.js`. The new test compiles an original-node-shaped1035 reward fixture, starts the actual quest authority, admits the exact gated drop, credits its inventory instance, consumes it on completion and awards exactly one hat. Separate cumulative-boundary checks cover zero weight, unequal weights, job filtering, preview RNG isolation and unsupported fractional weights. Native recipe `tutorial-drops` starts with no quest or loot, kills a Sentinel for the legitimate no-drop branch, accepts Todd through native dialogue, kills again, picks up with Z and reloads the durable result. Scenario registration and integrated execution are Main-owned; no browser pass is claimed during the concurrent edit batch.

A scoped production-only Bun smoke exercised `QuestSystem`, `DropSystem` and `ProfileStore.memory` with the exact existing1035 record explicitly marked admitted to isolate the new runtime consumer; it did not re-extract or mutate the release catalog. It observed inactive drop count0, native-authority begin with0 RNG draws, active drop count1, durable4031802×1 pickup, then completion consuming the Shellpiece and granting1002069×1 plus30EXP with quest state2. The total was two injected draws: one drop roll and one hat selection. This is rendererless transaction proof with an explicit admitted-record fixture, not extraction admission, native browser input or Windows proof; the source-shaped extraction regression and native scenario still require Main's integrated run.

### Local multiplayer benefit consumers

[`local-social-family.js`](../client/src/social/local-social-family.js) executes explicit local family authority using Cosmic entitlement IDs, costs, durations and rate data. Its own comment records that Cosmic's `FamilyUseHandler129` is incomplete: implemented offline rates are not a claim that this server handler or the original client supplies the entire transaction. A native family confirmation must pass current reputation, once-per-calendar-day usage and attached capability admission before atomic consumption. Personal rates target self; party rates target loaded same-map party members; Family Bonding requires at least six loaded descendants and applies EXP/drop to the actor plus those descendants. Same-kind benefits replace that kind, expire by their saved wall-clock deadline and then read as1×.

Actual field kills use `trunc(originalEXP * admittedFamilyRate)` once. `DropSystem` multiplies admitted family/card rates into chance only. Trusted actual kill/boss/level events can earn senior and grandparent reputation (4/20/200, halved when the senior is lower-level; maxHP≤1 kills excluded). Peer UI commands cannot call that progress producer as a fabricated kill. Family reunion and consented summon use the local travel authority; no remote server, remote world simulation or automatic party EXP-sharing algorithm is implied.

The [first-pass social report](native-ui-validation/social/report.json) used explicitly seeded3000 reputation to exercise native My Drop Rate1.5×: Cancel kept3000; confirmation consumed700, stored the15-minute personal effect and survived reload. That is benefit purchase/persistence proof, **not earned reputation, changed drop odds, party benefits, bonding or expiration proof**. Final family coverage belongs to its separately identified replay, indexed in [validation](validation.md).

The [final frozen-build Family report](native-ui-validation/final/family/report.json) records source `eb84f90bc737325848d220c995dd11c6c3da05b12d97309c687cfde787036983` and catalog `273cc7e6d1835af5323ac6d26b270ab10094971ef63ccdfb9acdf0c8e7b942f1`. It exercised all11 entitlements using an explicit50000 reputation budget, precise party inclusion/exclusion, the five-versus-six-descendant bonding boundary and reload durability. Actual Blue Snail killing awarded EXP0→6 under1.5× and senior reputation0→4. With already-purchased2× deadlines explicitly shortened, actual same-template kills awarded+10 before real expiry and+5 afterward. A near-level EXP fixture followed by a real kill produced level30→31/EXP4 and senior reputation12→216 (kill4+level200). These are earned _transitions after disclosed setup_, not acquisition of the seeded budget/roster.

That final report retains one failure: native incoming summon to Henesys failed on duplicate `sp` destination names while preserving the request/source state. A paired unique-destination summon succeeded. The initial HUD-level allegation was a screenshot misread; enlarged exact-pixel inspection confirmed HUD31, matching the real level31/EXP4 commit. Drop probability scaling was not statistically measured; neither full15/30-minute waits nor actual midnight/DST crossing were performed. Expiry used shortened saved deadlines, and day reset used prior-day markers with an unchanged actual clock. No later source-fix equivalence is implied.

### Operational gameplay admission

Native field actions require the current live scene and input owner, and reject death, field replacement, CashShop ownership, unrelated modal/carry state and pending field/profile operations. Modal blocking prevents _new_ movement/jump/attack/pickup/portal intent; existing airborne hit inertia may still settle and is not evidence of admitted locomotion. Field reveal releases movement at accepted commit, before its600-ms incoming brightness completes. Keyboard and inventory item use share `ItemUse`; its repeated admission excludes **its own** pending operation while preserving other owners' gates. This corrects the first-pass bound-potion self-block without removing the transactional recheck.

Chat/native editors consume their own Enter/Escape and clear held gameplay state on focus changes; repeat/IME events cannot fall through as ordinary submit/attack. Native keyboard/mouse takeover revokes agent control without synthesizing actions. The [diagnostic gameplay report](native-ui-validation/gameplay/report.json) exercised an alive NPC modal, settled death-modal input refusal, nondismissable Revive and confirmed authored return-map recovery to50HP with movement during reveal. It also preserved the then-failing bound-potion case; no final pass is inferred solely from the corrected source.

## Dynamic resource ownership

`packageMap` keeps mob pixels in the map texture/atlas closure but excludes mob entities from static streamed regions. `manifest.life.renderables[template]` contains `{entity,atlases,bounds}` using original frames/parts and normal atlas descriptors. Descriptor bounds are **presentation demand extents only**, never hitboxes. Every template is represented once, not once per mob. Main also preserves `scene.combat` and `scene.reactors` in the map manifest.

`OfflineMobRenderer` owns `VisualTextures` per demanded template and calls scene `addDynamicEntity/removeDynamicEntity`; the scene owns display membership, not those textures. A viewport leaving an authored static cell cannot reset mob position/HP/death/timers. Far-away artwork is removable while the full authored mob simulation continues; re-entering demand constructs original artwork and seeks directly to the existing simulation phase. The normal atlas CPU/GPU budgets and global scene sprite/entity ceilings still apply. Failed loads retain explicit scene/renderer errors rather than claiming residency. No per-tick fetch, load scheduling, allocation of actor/rectangle state, sorting, or resource mutation is performed.

## NPC interaction admission

World-pointer NPC admission uses the original translated, unflipped `dc` rectangle and primary-button release; the cursor uses the same live picker. `006dd584..006dd6f0` defaults each absent edge to left−22/top−65/right22/bottom0; `006d3fde` translates without mirroring. The old guessed120×100 player-distance gate is removed. Death, modal ownership, hidden/unresident artwork, stale membership and unsupported imitation actors reject admission. Inspector selection never opens gameplay dialogue. Continuations recheck the original NPC owner and may pass their own live dialogue modal; new world openings cannot. These live browser ownership gates do not recover every original actor/controller gate. See [NPC consumers and limits](ingame-life.md).

## Skill and projectile capability boundaries

All **534 numeric player skills /71 books** are catalogued; [current skill coverage](skills.md) distinguishes485 source-capable classifications from49 unavailable records and from actual native replay. Ranged/magic projectiles, ammunition attacks and admitted summon controllers are no longer blanket-unavailable. Ordinary and applicable skill rays search20px strips with distance/4 vertical expansion against original current mob bodies; acquired flights aim at body centers, retain target-generation ownership and still render when no target exists. Magic Claw's lack of an authored ball is preserved. [Ranged consumers](ghidra-client-corrections/reported-r3-weapon-basic.txt), [flight consumers](ghidra-client-corrections/reported-r3-weapon-flight.txt) and [seeking rays](ghidra-client-corrections/native-skills-chain-ray.txt) retain provenance. Schema8 persists learned/master/expiration records and ten SP pools without granting ranks for job selection. `damagedBySelectedSkill` remains a real restriction. Unsupported controllers/scripts are explicit, and no projectile or summon is manufactured from artwork alone.

## Executed verification

The [final native gameplay report](native-ui-validation/final/gameplay/report.json) records the same `eb84f90bc737325848d220c995dd11c6c3da05b12d97309c687cfde787036983` source and `273cc7e6d1835af5323ac6d26b270ab10094971ef63ccdfb9acdf0c8e7b942f1` catalog as the Family report. Its gameplay slice has no remaining reported failures: all four prior effect-placement/raster/tooltip/bound-potion defects pass. Native D consumed one explicitly seeded Red Potion, HP40→90 and same-UID count2→1. Safe-ground recovery sampled MP10→13 at10020ms and HP20→30 at15120ms; hostile-field contact sampled positive hits at30/1530ms, timer expiry at1500/3000ms, ordinary−270/−270 launch, and later natural HP482→492 at16500ms then maximum500 at26520ms. The exact earlier hostile-field brace-to-standing transition was not continuously recorded.

Final native recoil coverage is322 below `pushed800`,907 with `hit1`/hit600/speed130, and1112 damage while the already-running mob `attack1` retained frame4/actionMs660; a separate trace measured130→118 over30ms. This proves admitted recoil and retained pose, not the pending attack's eventual damage. Seeded learned rank1 Stance was natively cast; its42 probability expired by simulated10020ms, after which positive hits with rolls16/9 launched instead of resisting. Exact799/800, repeat-hit deadline refusal, strong type2, ladder/sentinel/nonpositive incoming outcomes, revival load failure and ten-minute auto-confirm remain outside this final browser slice. Its death/Revive test observed settled modal input refusal, authored town recovery to50HP/30MP and input released during the600-ms reveal. Explicit fixtures are not ordinary acquisition; no original Windows parity follows.

The focused rendererless [conditional-knockback proof](ghidra-client-corrections/knockback-provenance.json) executes production `OfflineField`, mob and physics consumers against extracted original map211040000: Hector damage799 loses HP without recoil;800 admits recoil while preserving attack1; a repeated800 within the hit deadline loses HP without reversing/restarting motion; a supplied roll29 against weapon probability30 selects300px/s, braking to294 after30ms. Player misses, direction sentinel and resisted positive hits keep original foothold149; admitted−270 motion lands on original foothold125; resisted ladder damage keeps the ladder while admitted+270 detaches it. This is a scoped runtime smoke, **not native keyboard/browser or original Windows proof**. No formatter, build or test suite was run by this worker.

The [expanded historical gameplay pass](ingame-validation/expanded/gameplay/report.json) exercised Hector798 versus800 and Red Snail32 under the former deterministic local damage formula. Do not reuse those STR/DEX recipes as current guaranteed damage: the recovered generator samples per-target ACC/mastery/PDD and level effects. The later [native gameplay diagnostic](native-ui-validation/gameplay/report.json) measured Hector322 below `pushed800` and907 with ordinary130px/s recoil. Its seeded learned Stance rank1 was cast through native D, with positive resisted hits and a separate unresisted roll. Explicit learned records/equipment/scenario positions are setup, not normal acquisition. Exact799/800, repeated-hit deadline, attack-pose impact retention and strong type2 remain outside that diagnostic's native coverage; the rendererless proof above is not upgraded to browser acceptance.

The [historical interaction report](ingame-validation/interaction-corrections/report.json) records native contact hits1500ms apart with the **then-current±200/−200** impulses, complete60-ms tint phases, Red Snail HP40→8 and receiver geometry. The ordinary impulse is now±270/−270;200 was the misidentified Power Guard branch. Blue Snail loot, quota/capacity rollback, accepted pickup audio and durable reload remain that report's scoped evidence. The [later diagnostic](native-ui-validation/gameplay/report.json) observes current ordinary−270/−270 hits exactly1500 simulated ms apart, two-tick RGB phases and separate brace expiry. Neither source's observations are silently promoted to final-source or Windows parity.

The [native hit/Shroom correction proof](client-corrections-validation/hit-shroom.json) and [180-sample trace](client-corrections-validation/hit-shroom-trace.json) cover map **104040000**. All ten Shrooms moved through authored floor links without faults. Two observed positive hits were exactly **1500 simulated ms apart**, each exposing **(+200,−200)** velocity and unlocked movement. Native Pause captured gray and normal tint at remaining timers1350/1320 ms. Map selection prepared the scenario; movement, drop-through, recovery and pause used actual keyboard/UI input, with snapshots observing rather than mutating gameplay.

![Original gray hit tint during airborne knockback](client-corrections-validation/hit-gray.png)

![Normal tint on the next original update while still protected](client-corrections-validation/hit-normal.png)

The frozen combat milestone passed strict scoped ESLint and **45 tests /298 assertions**. These captures precede the graphical UI/binding cutover; their old HUD is not evidence for the later UI work.

Original WZ/Ghidra investigation established the swordOL bank, weapon inputs, Octo restriction, Hector type-0 range/timing/MP cost and unrecovered EXP-table boundary. Historical [independent native combat/quest evidence](offline-validation/combat-quests/evidence.json) exercised actual movement/attack keys, mob damage/death/respawn, local progression, player death/recovery and Camila's 30-Pig completion with EXP600 exactly once. The historical [recovery/reactor replay](offline-validation/main/recovery-reactor-after.json) confirmed the **then-current full-recovery path**, restored canvas focus and a real accepted reactor hit without fault; it does not prove current revival. [Loading replay](offline-validation/main/loading-alive-after.json) held Right+X against an intercepted replacement: the old scene remained visible, but coordinates/ticks/HP stayed fixed until atomic commit.

These older captures remain historical browser local-policy results, not original-server combat parity. Their full-restore recovery predates ordinary confirmation/return-map revival, and older deterministic damage,200px/s impulse and provisional drop curves are not current contracts. Use the explicitly source-identified native gameplay/social reports above and [validation](validation.md) for final replay coverage and remaining boundaries.
